# Application environment
APP_ENV=dev
APP_DEBUG=1

# Clé secrète de l'application (placeholder pour dev)
APP_SECRET=dev-secret-placeholder

# URL de base pour les commandes CLI et génération d'URLs
DEFAULT_URI=http://localhost

# Database configuration (à remplacer localement)
# Remplacez user, password et dbname selon votre setup local
DATABASE_URL="postgresql://user:password@127.0.0.1:5432/dbname?serverVersion=16&charset=utf8"

# Messenger transport (doctrine pour dev/test)
MESSENGER_TRANSPORT_DSN=doctrine://default?auto_setup=0

# Mailer (dev)
MAILER_DSN='smtp://localhost:1025'
MAILER_FROM_ADDRESS="noreply@exelhunt.local"
MAILER_FROM_NAME="ExelHunt"

# CORS settings (dev/local)
CORS_ALLOW_ORIGIN='^https?://(localhost|127\.0\.0\.1)(:[0-9]+)?$'
